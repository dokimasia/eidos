// Copyright ThesmOS B.V. 2026
// SPDX-License-Identifier: Apache-2.0

// Code generated by internal/gen/model. DO NOT EDIT.

package emit

import (
	"testing"

	"go.dokimi.dev/assert"
)

// A slot accessor is the composition seam between plugins, so what
// it hands back is contract: the value's own storage, never a copy
// and never nil.
func TestSlots(t *testing.T) {
	t.Parallel()

	t.Run("Enum.VariantsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Enum{}
			slot := subject.VariantsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&EnumVariant{})
			assert.Equal(t, subject.Variants.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.VariantsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Enum.FieldsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Enum{}
			slot := subject.FieldsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Field{})
			assert.Equal(t, subject.Fields.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.FieldsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Enum.MethodsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Enum{}
			slot := subject.MethodsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Method{})
			assert.Equal(t, subject.Methods.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.MethodsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Sum.VariantsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Sum{}
			slot := subject.VariantsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&SumVariant{})
			assert.Equal(t, subject.Variants.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.VariantsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Sum.MethodsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Sum{}
			slot := subject.MethodsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Method{})
			assert.Equal(t, subject.Methods.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.MethodsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("SumVariant.FieldsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &SumVariant{}
			slot := subject.FieldsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Field{})
			assert.Equal(t, subject.Fields.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.FieldsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Struct.FieldsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Struct{}
			slot := subject.FieldsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Field{})
			assert.Equal(t, subject.Fields.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.FieldsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Struct.MethodsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Struct{}
			slot := subject.MethodsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Method{})
			assert.Equal(t, subject.Methods.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.MethodsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Struct.TypesSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Struct{}
			slot := subject.TypesSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Struct{})
			assert.Equal(t, subject.Types.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.TypesSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Interface.FieldsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Interface{}
			slot := subject.FieldsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Field{})
			assert.Equal(t, subject.Fields.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.FieldsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Interface.MethodsSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Interface{}
			slot := subject.MethodsSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Method{})
			assert.Equal(t, subject.Methods.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.MethodsSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})

	t.Run("Interface.TypesSlot", func(t *testing.T) {
		t.Parallel()

		t.Run("hands back the value's own storage", func(t *testing.T) {
			t.Parallel()

			subject := &Interface{}
			slot := subject.TypesSlot()
			assert.NotNil(t, slot,
				"the slot is part of the value, so the accessor never returns nil")
			assert.Equal(t, slot.Len(), 0, "and an untouched slot holds nothing")

			slot.Append(&Interface{})
			assert.Equal(t, subject.Types.Len(), 1,
				"what a plugin appends through the accessor is what the value holds")
			assert.True(t, slot == subject.TypesSlot(),
				"and a second call addresses the same storage, so two plugins "+
					"appending through it append into one list")
		})
	})
}
