# Decision log

*Every decision, why we made it, and what would make us reconsider.
The document linked in each row carries the full argument.*

This table lists every settled decision, with one line of reasoning
and, where we recorded one, the event that would make us reconsider.

Look here first. Write a full ADR in [docs/adr/](../adr/README.md)
when that event actually happens, or when the reasoning no longer
fits in one row, and then link the row to it.
[ADR-0001](../adr/0001-use-adrs-for-architecture-decisions.md) says
why we work this way.

| # | Decision | Why (short) | Revisit trigger | Doc |
|---|---|---|---|---|
| D1 | One repository, one module per component: a kernel module plus satellite modules, tagged independently, all written in Go | the module boundary follows change rate. Models and contracts move slowly and breaking them is expensive, while catalogs and language support change often on their own per-module cadence | none | [01](01-repos-and-kernel.md) |
| D2 | eidos is a library and ships no binary, ever | consumers' binaries are the products, and the command kernels give them a uniform surface for free | none | [14](14-distribution-and-cli.md) |
| D3 | Treat this as a framework outsiders use: stability checked by machinery, documentation shipped with the release, conformance as the instrument | external adoption changes the engineering rather than only the marketing. Every schema and flag becomes API somebody scripts against, so stability has to be checked rather than intended | none | [15](15-compatibility.md) |
| D4 | Two concrete models, node and emit, generated from one symbol schema | a merged model loses compile-time phase discipline and multi-plan safety. Embedding dies on covariant recursion. Generating both stops them drifting apart | none | [02](02-symbol-model.md) |
| D5 | Projection tiers, with a rule about where a question belongs | without that rule, a question arrives wherever the first person to ask it was standing | none | [03](03-projection.md) |
| D6 | The vocabulary anticipates Rust, Python, Java, Kotlin, PHP, C# and Swift now | retrofitting variance, async and error models later would touch every detector and every lowering. PHP forces no new element, since its unions, attributes and enums are already in the vocabulary | none | [03](03-projection.md), [11](11-languages.md) |
| D7 | protobuf is read-only, permanently | a schema language is read and never written, and the satellite anatomy makes read-only a complete shape | none | [11](11-languages.md) |
| D8 | One workspace, N plans, one backend per plan. Plans are values rather than plugins | per-plan invariants such as imports, formatting and sweep each need exactly one answer. Plans own what has to be enforced between plugins | none | [08](08-workspace-and-plans.md) |
| D9 | No daemon and no client-server protocol: batch plus `--watch`, over a lazily loaded sealed-graph format | format design meets the performance target. A protocol becomes public API and adds a state-drift tax, and no client needs a resident process | somebody building an IDE surface | [09](09-incrementality.md) |
| D10 | Incrementality is red-green with early cutoff at (symbol, key) grain | bags are fat and plans are many, so per-symbol invalidation would re-run unrelated plans. The bookkeeping costs O(reads) | none | [09](09-incrementality.md) |
| D11 | Cross-language conversion goes hub-and-spoke through canonical shapes, with policy as data and no bridge modules | pairwise walkers cost O(pairs) and drift apart. A hub makes each language map once | none | [10](10-cross-language.md) |
| D12 | No CUE anywhere. text/template plus the lint check and the builder as an equal path, with YAML and a published JSON Schema for config | CUE is a configuration language and does not render text. Config gets its typing from the schema without asking consumers to learn a new language | lowering-policy tables growing real constraint logic | [07](07-rendering.md) |
| D13 | No rebuild tool | the people who would use it already have the Go toolchain, and users who do not write Go are served by consumer binaries and the declarative host | demand from consumers who cannot ship binaries | [14](14-distribution-and-cli.md) |
| D14 | One directive grammar, a carrier per language, and native sugar that lowers to the canonical form | N dialects fracture plugins, documentation and migration. Carriers keep each language's idiom | none | [05](05-directives.md) |
| D15 | The shape catalog is spec-first, with generated registries and detectors that read only projections | a directive that under-determines its check, and a rule that tests nothing, both become review-time checkboxes. The cost is 90 + N rather than 90 × N | none | [12](12-shape-catalog.md) |
| D16 | tree-sitter is the frontend platform: one grammar model and one binding layer, uniform across satellites. A parser is always a pinned library, never a toolchain the machine supplies. Two exceptions exist where a proven pure-Go parser already does the job: Go's stdlib `go/parser` and protobuf's protocompile | the requirement is hermeticity and cross-machine determinism, with uniformity where it costs nothing. A version-pinned parser is hermetic whatever its origin, and the kit keeps both exceptions invisible downstream. The native TS compiler is written in Go but ships internal packages only, so there is nothing importable to pin | a native compiler port publishing a public importable API for a heavy source language | [11](11-languages.md) |
| D17 | The performance target is monorepo scale: 10k+ packages, warm regeneration under a second, a memory-resident graph with signature-only dependencies | the target is chosen rather than derived. The largest consumers are monorepos, and a tool that degrades there gets replaced by five single-purpose generators. Stating it as a requirement is what licenses every engine trade-off downstream | none | [09](09-incrementality.md) |
| D18 | A workspace is a config boundary spanning toolchain workspaces. Several workspaces are declared explicitly, and nothing crosses between them | implicit nesting leaves two manifest owners that do not know about each other, which is how generated files end up orphaned | none | [08](08-workspace-and-plans.md) |
| D19 | Plan exports with declared dependencies, and reading a sibling plan's emit is forbidden | binding generators need generated names without drift, and undeclared edges destroy scheduling and parallelism | none | [08](08-workspace-and-plans.md) |
| D20 | Priorities are per role | one shared number cannot place a dual-role plugin's two halves independently | none | [06](06-plugins.md) |
| D21 | eidos maps interfaces, never implementations | this is the boundary that keeps conversion from becoming a compiler | none | [10](10-cross-language.md) |
| D22 | Diagnostic codes are stable and are API | consumers assert on codes rather than on message text, and a changed meaning is a new code | none | [15](15-compatibility.md) |
| D23 | C and C++ are out of scope | there is no declaration model worth projecting without a real compiler frontend, so this is refused rather than half-built | a funded compiler-frontend effort | [11](11-languages.md) |
| D24 | Five decisions that hold each other up: plugins compiled in, metadata as the only channel, slots, per-backend template ownership, and one funcmap per language | each one guards the others. Typed keys and a shared graph work only in one process, so plugins compile in. Facts need authority and provenance, so there is one channel. Shared output needs accumulation rather than override, so slots. Languages differ in structure, so backends own templates. Helpers need one documented name, so the funcmap registers once. Remove any one and it reopens the hole the next one covers | none | [06](06-plugins.md), [07](07-rendering.md) |
| D25 | Three severities, any Error fails the run, suppression is per code per position with an audit trail, and a kernel Error cannot be suppressed | suppressing silently or globally hides real findings. Codes make a suppression precise, and `doctor` reports the ones whose code no longer occurs | none | [16](16-diagnostics.md) |
| D26 | The sink contract: write if changed, atomic per file, staged per plan, root-jailed, with no clocks or environment in the output, and UTF-8 with LF everywhere | rewriting unchanged files breaks consumers' build caches. Partial writes corrupt trees. Timestamps break byte-determinism | none | [17](17-output-and-determinism.md) |
| D27 | Routing: two layouts plus tagged outputs, refined per plugin and per tag, with kernel-owned per-declaration overrides, and unmanifested files never overwritten | what the source author writes outranks config, which matches the authority model. Proving ownership before overwriting or deleting is what makes sweeping safe | none | [18](18-routing-and-layout.md) |
| D28 | Benchmark gating splits by what depends on the machine: scaling-rule and allocation gates run everywhere, absolute-time gates run on dedicated runners with significance testing, and corpora are seeded synthetic | timing on a shared runner flakes, and a flaky gate teaches people to ignore it. Ratios and allocation counts are noise-free, and real repositories cannot be parameterized | none | [19](19-benchmarking.md) |
| D29 | A frontend never executes project code or build tools. Module identity that only execution could compute is supplied in config | hermeticity and supply-chain safety. A generator that runs Gradle has become a build step rather than a reader | a sandboxed evaluator design | [11](11-languages.md) |
| D30 | Drift detection: a manifested file whose hash on disk differs is refused rather than overwritten, and `prune` never deletes it | the manifest already holds content hashes, so a hand edit is detectable. Overwriting it silently loses somebody's work | none | [17](17-output-and-determinism.md) |
| D31 | Signature-only loading reads binary declaration formats where an ecosystem distributes no source, such as JVM class files and .NET assembly metadata, parsed and never executed | JVM dependencies arrive as JARs. A source-only reader would parse the whole workspace and resolve none of its dependencies | none | [11](11-languages.md) |
| D32 | A per-release schema index, with evolution classified mechanically. A breaking schema change blocks the tag outside a major, and `doctor` runs the same diff on the consumer side | annotations in consumer source are the surface least able to absorb change, so "only adds" has to be checked on schemas exactly as it is on APIs | none | [15](15-compatibility.md) |
| D33 | No per-plan model transforms, and whole-file ownership only: eidos never merges into a file it does not wholly own | transforms fork the one graph and break explain, invalidation and the cross-plan checks. Owning half a file gives you no determinism, sweep or drift guarantee | none | [08](08-workspace-and-plans.md), [17](17-output-and-determinism.md) |
| D34 | A plan export is a versioned kernel schema recording each symbol's kind, canonical signature, and the spelling its lowering chose | the producing plan's lowering is the only authority on what it named things, so dependents read spellings rather than recomputing conventions | none | [08](08-workspace-and-plans.md) |
| D35 | Values in Tier 1: two distinct samples, a zero literal, and literal-for-text, all of which refuse rather than guess | check generators need values they can tell apart, because a check against one value passes whenever the subject already held it. A guessed literal is worse than a refusal | none | [03](03-projection.md) |
| D36 | The scaffolding body vocabulary stays small: a delegate call, a return, an assignment and a guard, growing only by kernel decision | it keeps "interfaces, never implementations" a mechanical boundary rather than a judgement call. Logic belongs in a hand-written file the generated one calls | none | [07](07-rendering.md) |
| D37 | Render dispatch: the backend's kind templates render everything by default, a plugin claims its own files per declared output, and slot contents always render through the kind machinery | a plugin that only assembles values ships no templates and still renders idiomatically, and no plugin's template ever renders another plugin's values | none | [07](07-rendering.md) |
| D38 | Mixing granularity runs statements, then a body through `TemplateRef` resolved in the emitter's tree at render time, then a file, and nothing between declarations. `Verbatim` exists only inside bodies | generators name templates and never execute them, which keeps them never seeing languages. Declarations stay structured for slots, merging, imports and lint, and free-floating text among typed declarations defeats all four | none | [07](07-rendering.md) |
| D39 | A body is a slot sequence: `prologue` and `epilogue` on every body, owner-declared slots between them, and a body-claiming template must place the marker, which lint checks. Composition is three verbs: append through slots, replace through template overrides, and wrap, which the render level refuses and the model level does. Slot declarations generate from the schema | plugins compose bodies without touching each other's templates. Standard slots remove the need for the owner to anticipate extension. Wrapping at render level is order-sensitive semantics nothing can defend | none | [07](07-rendering.md), [02](02-symbol-model.md) |
| D40 | Canonical symbol identity is package path, kind, name and a signature discriminator, stable across reparses | read sets, invalidation, exports, provenance, drift and explain all reference symbols across runs. Without a stable identity the engine cannot be built, and overloads make names insufficient | none | [02](02-symbol-model.md) |
| D41 | Naming and lowering facts stamp during Annotate, through target-language annotators the workspace registers automatically. A plan never writes the shared graph | plans run after freeze, in parallel, and read-isolated, so a plan writing shared bags breaks all three. A name is a fact about a symbol and a target language rather than about a plan | none | [10](10-cross-language.md) |
| D42 | Adopt an unmanifested path when its bytes match, with `--adopt` for the rest. One writer per workspace, and a concurrent run fails immediately | adopting the tool must not fail on the first file. Two runs racing one manifest leaves generated files nothing tracks | none | [17](17-output-and-determinism.md) |
| D43 | The directive grammar is pinned in the specification: EBNF, one quoting form with double quotes and four escapes, continuation through a trailing `\`, and flat keys | the surface least able to absorb change gets the most exact specification. A deferred grammar invites carriers to diverge | none | [05](05-directives.md) |
| D44 | Strings only where humans type them. Every registered name is a typed value or a generated constant inside the type system, and every human-typed string resolves against its registry at workspace Build | it turns a whole class of typo into a compile error where code is involved, and into a named Build error at the boundary. Generated constants mean nothing hand-written can drift from a registry | none | [README](README.md) |
| D45 | A two-layer plugin surface. The SPI roles stay the infrastructure contract, and authors write against the root-package facade: declarations as a value, triggers indexed by the symbol model's subject kinds and generated with the schema, plus `OnEmit` and `OnGraph`, `Directive` and `Where` for scoping, effects chosen by the handler's signature, uniform `(match, effect) error` handlers, and `skip` honoured by dispatch. Every rule set lowers to the SPI | the walk-filter-bookkeep routine and its correctness lessons, such as receiver collision and import inference, move into the framework once instead of into every plugin. Kinds describe structure and facts describe meaning, so no trigger is named after a semantic category. The lowering is conformance-tested to byte-equal output, and the anti-sprawl rule guards mechanisms rather than coverage of kinds | a plugin family the kind-indexed triggers and the wrappers cannot express | [06b](06b-authoring.md) |
| D46 | Output cardinality is declared per family as `PerSource`, `PerPackage` or `PerPlan`. Every emitter target is an accumulator keyed by cardinality key and family, ordered by canonical subject identity. Multiple files per plugin are families crossed with keys, and provenance is plural and independent of the filename derivation | one source producing one output was the demo's shape rather than the domain's, since even `PerSource` accumulates when two interfaces share a source file. An aggregate that depended on match order would break warm≡cold under parallel dispatch | none | [18](18-routing-and-layout.md), [06b](06b-authoring.md) |
| D47 | Resolution is Link then Lookup. A kernel Link step after Load resolves type spellings to canonical identities, assisted by the language, recorded neutrally, and degrading to spelling-only for builtins and externals. `Reader.Lookup(Identity)` is the join afterwards, through tracked reads. A human's textual name resolves through Tier-1 `Resolve` at validation. Cross-plan resolution does not exist | cross-package resolution needs the whole graph, so it is a phase rather than an on-demand walk. Using identity as the join makes a dependency-package lookup identical to a local one, and turns every cross-package read into an incrementality edge for free | none | [02](02-symbol-model.md) |
| D48 | There is no Transformer role. Rewriting source, transforming the graph and rewriting emit are all refused, and each use case maps to a mechanism that composes: replace, model-level wrap, projections and facts, frontends, or tooling at `manual` authority | the graph is input truth and every emitted value has one owner. Each transform variant destroys determinism, provenance or output stability once another plugin is added. The mapping table returns the question without reopening the argument | none | [06](06-plugins.md) |
| D49 | Plugins are target-universal in the neutral lane, meaning the builder, the scaffolding vocabulary and kind templates serve every target. Presentation layers as options: plugin-level `Templates` and `Funcs` as defaults, `For(target, …)` for specializations, and `Overrides` as the replace verb. There is no per-target word map, since filenames take the family word with the target's join and extension while identifiers go through the Emitter's `JoinName`. An undeclared target fails at Build, and one directive gates many rules and registers once, with conflicting redeclarations refused | this applies the Source and Target split to the authoring API itself: read-side questions on the Match, write-side spellings on the Emitter. A target fact in a neutral declaration is a leak that Go-to-Go development hides and a cross-language plan exposes. A word map smuggles presentation into a side channel when templates and helpers are the presentation surface | none | [06b](06b-authoring.md) |
| D50 | Routing is addressed rather than inferred. Families are independent accumulators, the handler names the family at each emit site, tags carry meaning, and the target spells the join of word and tag. `TagTest` is a kernel well-known tag a target may treat as its ecosystem does, giving `suite_test.go` against `suite.test.ts`, and an unknown tag gets the deterministic default join | routing by content would be guessing. Writing `_test` into a declared word leaks the extension one level up, since being a test is semantics the plugin declares and the ecosystem spells | none | [18](18-routing-and-layout.md), [06b](06b-authoring.md) |
| D51 | Annotators use the same rule grammar with the Stamper effect: directive-driven, fact-gated or bare. `Stamp` binds authority and origin in advance, a Stamper writes only to its subject's bag, `Fact` reads are tracked, and the builder surfaces `Provides` and `Requires` | one grammar to learn rather than two. Writing only to the subject makes every write's target statically known from the trigger, which keeps invalidation and audit mode analyzable. A fact "about" a sibling is a fact on the host that names the sibling | none | [06b](06b-authoring.md) |
| D52 | Schemas declare repeatability. Single-instance is the default, and a duplicate is a validation Error naming both positions. `Repeatable` runs the handler per instance in source order. `m.Directive()` is always the gating instance, and nil for bare and fact-gated matches. Other plugins' directives are invisible to a handler, and the ordering rule extends to subject, then instance order, then insertion | whether a second annotation contradicts or repeats is per-directive semantics only the schema can settle. Keeping other plugins' directives invisible stops their params becoming public API for other plugins. Instance-order determinism survives parallel dispatch | none | [05](05-directives.md), [06b](06b-authoring.md) |
| D53 | The frontend kit: one `CommentSyntax` per language shared with the backend kit, include-shaped selection that classifies rather than excludes so test files are parsed and stamped, the language-defined Unit with `u.Read` as its only filesystem door, jailed and fingerprint-tracked, and the same `Parse` running at `Depth Signatures` | comment extraction has to survive doc-block gutters and docstrings across the landscape. An exclusion hardcodes a decision that belongs to the consumer. The single door turns hermeticity and cache completeness from rules into mechanisms. The one exclusion the kernel owns is the workspace's own outputs | none | [11](11-languages.md), [09](09-incrementality.md) |
| D54 | The backend kit: a `FileTemplate` skeleton with a kit default, `KindTemplates`, `Funcs`, an `Imports` renderer, `Finalise`, and the shared comment syntax for the header. Merging, splicing, `TemplateRef` resolution and the `CodeRender` flow belong to the kit | import grouping and file skeletons are language facts, but the header rule, the format-error rule and the merge order have to be kernel behaviour tested once rather than reimplemented differently per satellite | none | [11](11-languages.md) |
| D55 | Gates are declarative: `Directive` and `Where` compose over every trigger, and `Where` over `OnEmit` evaluates the origin. Dispatch is an indexed one-pass engine, by kind, by directive name at Freeze, and by fact key at stamp time. The gate tuples double as the incrementality engine's subscription records | a gate inside a handler is a scan and a filter dressed as a subscription. Selectivity the engine cannot see degrades dispatch into O(graph) walks and invalidation into running too much. A rule has to cost O(its matches) | none | [06b](06b-authoring.md), [09](09-incrementality.md) |
| D56 | The performance commitments: stat-first fingerprinting that hashes only on suspicion, dense run-local IDs for identities, keys and paths with strings only at boundaries, read sets as packed ID pairs deduplicated per artifact, small-slice bags over interned key IDs, per-file render parallelism owned by the kit, and Link appearing in the per-phase benchmark attribution | the asymptotics are sound by construction, so the residual risk is constants and memory. Each commitment removes the largest known constant in its layer before the RSS and time gates find it the hard way | none | [09](09-incrementality.md), [11](11-languages.md), [19](19-benchmarking.md) |
| D57 | Deleting a fact is a kernel `meta` form. `meta drop=<key>` removes a fact at `directive` authority, outranking a `plugin` stamp and losing to `manual`, and `explain` shows it as a deletion somebody authored | a boolean is stamped only when true and absence is the negative, so negating a stamped fact has no set-a-value spelling. Without drop, the only recourse is `skip`, which withdraws the whole plugin instead of one fact | none | [04](04-metadata.md), [05](05-directives.md) |
| D58 | Tier-2 `EqualityRules`: `Comparable(ref, resolve)` reports whether a type works where the language demands equality, and names the member refs that break it | generators building map keys, switch arms or deduplication otherwise re-derive comparability per plugin, and get the recursive cases wrong, such as a struct containing a slice two levels down. The language owns the rule and names the offending members for the diagnostic | none | [03](03-projection.md) |
| D59 | Two standing refusals: the ecosystem directive prefix is permanent and never configurable per plugin or per workspace, and there is no symbol-level run narrowing | a configurable prefix forks the carrier grammar per workspace and breaks cross-repo tooling and grep, for no expressive gain. Symbol-level narrowing duplicates what dirty-driven regeneration already does, and we revisit it only for a profiled workflow where dirty-driven regeneration is demonstrably not enough. The per-package `go generate` workflow is served by pattern narrowing through `run .` | none | [05](05-directives.md), [09](09-incrementality.md) |
| D60 | The runtime architecture: an immutable `Workspace` holding the composition, registries and compiled dispatch plan, against a per-invocation `Run` holding the ledger, graph, facts, dispatcher, plan executions and diagnostics. The seams are Go contracts. Commit is two-phase, staging everything, then plan commits, then the ledger's `CommitRun` strictly last. Sealed state is generational and includes the fact store, and a dry run is a Run whose staging is discarded | listing components is not an architecture. The seams, the granule and the commit protocol are. Running `CommitRun` last makes every crash window conservative, so the worst case is a coldish rerun rather than recording something stale as clean. Persisting facts is what makes warm annotation O(dirty) instead of O(subjects) | none | [08](08-workspace-and-plans.md), [09](09-incrementality.md) |
| D61 | The artifact is the re-execution granule, and checks read records. The ledger's artifact table decides dirtiness by lookup, a dirty artifact re-runs its whole contributor set, and a clean one carries its manifest row and executes nothing. `WorkspaceCheck` reads manifests, exports, graph and facts, never raw emit | emit is never persisted, so any warm path needing sibling emit is a design error. The artifact table returns whose inputs moved without rerunning anything, and record-based checks behave identically cold and warm, including over carried plans | none | [09](09-incrementality.md), [08](08-workspace-and-plans.md), [06](06-plugins.md) |
| D62 | Outputs are never inputs. Load excludes workspace-owned paths, proven by a manifest entry or a provenance trailer, at the fingerprint gate. Foreign generated files are classified rather than excluded | reading its own output feeds generators their own results and breaks warm≡cold. The scope vocabulary has no negation, so no consumer could spell the exclusion. Ownership proof is decidable per file, and declared output globs are only a pre-filter, since `out=` redirects and the orphans of a removed plugin match no declaration | none | [08](08-workspace-and-plans.md), [09](09-incrementality.md), [11](11-languages.md) |
| D63 | The provenance trailer: every file ends with `<brand>:provenance sha256:<body-hash>`, and overwriting or deleting requires ownership proof through that trailer or a manifest entry. The manifest lives uncommitted in the brand's state directory, and headers carry the brand and source but never the command line | ownership carried in the bytes survives a clone where state does not, so first-run exclusion, adoption, drift and CI gates all work without a manifest. A committed manifest generates merge conflicts. A header stamped with the command varies with phrasing and defeats write-if-changed | none | [17](17-output-and-determinism.md), [20](20-cli.md) |
| D64 | Authored values: kernel-owned `sample` and `witness` directives stamp kernel-owned `gen.*` keys, and the Values and Witnesses returns read those stamps before deriving anything. The handlers ship as default annotators | a witness can be derived only from a trivially closed constraint such as `any` or `comparable`. Everything else is authored, or every check over the declaration is withheld. The preference has to be a contract, or satellites will disagree about it | none | [03](03-projection.md), [05](05-directives.md) |
| D65 | Tier-1 `Members`: the effective member set across Embeds, Extends and Implements, with per-member provenance and a reason for every contributor that yielded nothing | this is the first question every doubling generator asks. Promotion, override, merge and MRO are language rules, so anywhere but the language contract re-derives them wrong. The failure reasons let a generator refuse an incomplete double rather than shipping it silently | none | [03](03-projection.md) |
| D66 | Fact groups: a key registration may declare a bundle name, and `meta drop` accepts a key or a group, removing every member including stamps that arrive later | facts ship in families, and making an author list a writer's keys couples them to the internals the visibility rule hides. A declared group is one public name, resolved against the registry and typo-checked at Build | none | [04](04-metadata.md), [05](05-directives.md) |
| D67 | Schemas declare cross-directive `Requires` and `ConflictsWith`, validated by the kernel at Freeze over each subject's full directive list, naming both positions | the visibility rule leaves no plugin able to check another plugin's annotation, so a contradiction between annotations is caught by the schema layer or not at all | none | [05](05-directives.md) |
| D68 | No panic recovery: a plugin panic crashes the run with the plain stack | a panic is a defect rather than a diagnostic. Staging keeps the crash window conservative, since nothing partial arrives, and plugintest is where panics get caught before shipping. Recovery would turn defects into warnings people learn to ignore, on a run whose remaining output nobody can trust | none | [16](16-diagnostics.md) |
| D69 | Package identity under centralised layout derives from frontend-stamped module facts, with `importBase` in config where no module contains the output directory. Refusal arrives at the referencing declaration, and only when qualification is actually needed | an import path is not derivable from a directory alone, and a guessed path binds a bare name silently, which is the one failure a compiler will not catch. Refusing at the point of need costs a reference-free output nothing | none | [18](18-routing-and-layout.md) |
| D70 | Discovery and state are scoped by brand: a `.<brand>.yaml` config, a `.<brand>/` state directory beside it, and a lock per state directory | no eidos binary exists to claim `eidos.yaml`, and two consumer binaries in one repository must not read each other's config or race each other's manifest. What keeps them safe from each other's files is the ownership rule rather than the lock | none | [20](20-cli.md), [17](17-output-and-determinism.md) |
| D71 | A narrowed run sweeps only inside its narrowed source scope, or when a producer is removed. Out-of-scope manifest entries carry forward, `prune` reconciles the whole workspace, and `run .` under `go generate` is the supported per-package workflow | a partial run must not delete outputs it merely did not look at. `go generate` executes in the package directory, so pattern narrowing serves that workflow without reopening the refusal of symbol-level narrowing | none | [08](08-workspace-and-plans.md), [20](20-cli.md) |
| D72 | The sealed-state design: one generation directory holding the graph as an intern table, a per-unit region index and the regions, plus the fact store, the artifact table and a gating header, with a `CURRENT` pointer swapped on commit. A region is one frontend unit. Lazy regions are the read contract and commit I/O proportional to the dirty state is the write contract: generations share immutable content-addressed segments, a no-change run writes nothing, and memory-mapping and delta-over-base segments are permitted mechanisms. The fact store persists the claim record, envelopes, tombstones and losers included, because warm arbitration ranks against it. Encoding uses the standard library alone, and a mismatch or corruption falls back to cold with one Info | the parse granule, the lock granule and the invalidation granule collapse onto one grain. The state exists to make things faster, and something that can veto a run is a dependency instead, so damaged state must never fail a run that source alone could complete. A full-generation rewrite would scale the commit with the corpus and fail the warm-one-edit gate on its own | none | [09](09-incrementality.md) |
| D73 | Module identity is a neutral kernel fact: every frontend stamps `gen.module` and `gen.moduleRoot`, raw toolchain spellings stay in the frontend's own namespace, and Sources predicates, layout and lowerings read only the neutral keys | kernel machinery such as scope matching and centralised layout has to read module identity, and the kernel knows no language, so per-language keys would put a language table inside it. Normalizing while keeping the raw spelling is the same move Visibility makes | none | [08](08-workspace-and-plans.md), [18](18-routing-and-layout.md) |
| D74 | Facts live on the source graph only: `OnEmit` takes the Emitter effect alone, and Build rejects an `OnEmit` rule with a Stamper handler | emit is per plan and plans run in parallel, so an emit-side fact would live in a second universe that Close's records, the sealed state and sibling plans never see. A fact about generated output is a fact on its origin, stamped during Annotate | none | [06b](06b-authoring.md) |
| D75 | Exit codes: 0 on success, 1 when diagnostics failed the run, 64 on usage or config error. 2 is deliberately unclaimed, because a Go panic exits 2, so a crash is distinguishable from a typo without reading stderr | with no panic recovery, a panic's exit would collide with any meaning assigned to 2. Leaving 2 to the runtime makes every code mean exactly one thing | none | [16](16-diagnostics.md), [20](20-cli.md) |
| D76 | Build is a specified phase: a seven-step validation sequence running registries, lowering, options, plans, policies, dispatch compilation and the contract handshake, collecting every fault and reporting them together. The workspace builder, `Plan` and `ExportDoc` are pinned contracts | the phase the specification referenced most was the only one it never specified. The order matters because later steps assume resolved names, and collecting every fault is what lets a consumer fix a composition in one pass | none | [08](08-workspace-and-plans.md) |
| D77 | The parse memo: a second, optional persistence layer of content-addressed regions keyed by unit fingerprint and frontend version, consulted by Load between the sealed region and a reparse, size-capped and evicted least-recently-used at CommitRun. `run --cold` bypasses both layers, CI restores are safe by design, and there is no remote or shared cache protocol | the sealed state returns the edit loop and the memo returns branch switches and restored CI caches. A hit equalling a fresh parse follows from the key. An uncapped cache is a disk leak, and a cache protocol is the daemon argument again with a network attached | an organisation demonstrably running out of CI-restore bandwidth | [09](09-incrementality.md), [08](08-workspace-and-plans.md), [20](20-cli.md) |
| D78 | The Reader's grain rule: a targeted read, meaning Lookup, a member walk or a fact, records a per-identity edge, while an enumeration such as ByKind or a package list records a set-membership edge. Adding or removing a symbol re-runs enumerators, and changing one re-runs only its targeted readers. Edges are scope-filtered | this extends the gate-free rule's economics to reads, so no read costs more than it observed. Without it, either every enumeration re-runs on any change, which runs far too much at L scale, or invalidation silently misses additions | none | [02](02-symbol-model.md), [09](09-incrementality.md) |
| D79 | `run --check` is the CI staleness gate: dry-run semantics with a non-empty diff promoted to an Error diagnostic, exiting 1 through the ordinary contract | a dry run exits 0 on a pending diff by design, since a diff is not an error at review time, so CI would otherwise have to parse JSON to enforce currency. Promoting it to a diagnostic reuses the exit contract instead of minting a code | none | [20](20-cli.md) |
| D80 | The catalog is one classification mechanism with three declared forms, pinned by a `form` field in every spec: shape, which is single-valued, inferable and carries precedence; mixin, which is declared and accumulates; and contract, which is declared and groups roles with arity. Name constants are typed per form | the forms are the occupied cells of who asserts crossed with how many stick, and the form decides what nothing downstream can re-derive: which arbitration applies, the directive shape, and what a consumer generates. Collapsed into one flat namespace, every consumer would carry a which-kind lookup and first-claim-wins would break for mixins | none | [12](12-shape-catalog.md) |
| D81 | The plugin identity is one typed spelling: `Plugin.Name` returns `plugin.ID`, an alias of `diag.Origin`, and the claim, unit and finding envelopes carry the same type | three envelopes already carried one identity, and an untyped base contract forced a conversion at every boundary while letting any string pose as a plugin. The definition sits in diag, renamed to what that package owns, because the layers beneath the SPI cannot import it | none | [06](06-plugins.md), [16](16-diagnostics.md) |
| D82 | A gate reads its key when the rule is declared, and Build panics on a zero key. A handle a composition assigns later serves handler reads and writes through the closure, never a gate: a gate's key registers before the rule is declared | the subscription record carries the key as data at declaration, so a later-assigned handle is still zero there and the gate would silently watch nothing. The panic makes the wrong pattern loud at the first Build in any test | a plugin demonstrably needing to gate on a key its own provider registers, which takes lazy predicate resolution | [06b](06b-authoring.md), [04](04-metadata.md) |
| D83 | The projection walks are the kernel's: the member walk, the signature mapping and the type-shape fold live in `core/rules`, and a language returns the decisions inside them, which member lists contribute and how names shadow, the parameter and return roles, the meaning of a builtin spelling, what a spelling names, and the values | five languages would otherwise carry five cycle guards, depth budgets and gap vocabularies for four decisions each, the same duplication the backends' shared helpers removed | a language whose shadowing rule fits none of promote, override, merge and linearise | [RFC-0014](../rfc/0014-the-projection-vocabulary.md) |
| D84 | One closed enum, `symbol.TypeForm`, names the structure of a type reference and of a shape; the frontend sets the structural half on the reference beside its verbatim spelling, with children in a fixed order per form, and the kernel's fold adds the leaves | a projection cannot recover the identities inside a composite after the load, and two vocabularies for one structure drift the first time one grows | none | [RFC-0014](../rfc/0014-the-projection-vocabulary.md) |
| D85 | A sample is an emit value tree the backend spells and qualifies for the file it writes, carried by an `ExprValue` scaffold expression; the scaffold gains no statement kind | text cannot be qualified for another package and a nested composite cannot be qualified at all; the four statement kinds stay too small to write logic in | none | [RFC-0014](../rfc/0014-the-projection-vocabulary.md) |
| D86 | The `Constraint` kind leaves the schema: a Go constraint interface is an `Interface` whose type-set elements the frontend stamps, and a bound is what the parameter's `Bounds` carries | no frontend produced one, and the kind duplicated an interface with stamps | none | [RFC-0014](../rfc/0014-the-projection-vocabulary.md) |
| D87 | Every declaration kind that can end a source line carries a trailing `Comment`; `Embed` carries Doc, Comment, Tag and Annotations and is a directive subject; `File` carries Annotations; `Import` carries Doc and Comment | the write side spells a trailing comment in every target, an embedded field takes a doc line, a tag and a carrier like any field, and file-level tool directives and import comments had no home | none | [02](02-symbol-model.md) |
| D88 | A package's imports are the union of its files', computed by a helper on the node package; the model carries no package-level import list | the files record the scope as written, and a second list would drift from them | none | [02](02-symbol-model.md) |
| D95 | protobuf follows protoc wherever protoc decides: comments attribute the way protoc's source info attributes them, `features.field_presence` resolves down the file, message and field chain into a field's form, a well-known type never resolves against the graph, an enum value is its number converted to the enum type, and a literal reads in protoc's grammar. Name resolution diverges in one place: the tiers probe past a scope that declares only the first segment of a compound name, where protoc fails | a schema documented and compiled against protoc must mean the same thing here, and the one divergence changes nothing for a schema protoc accepts. A raw enum name is text only protobuf spells, and a converted number is a value every target spells | none | [11](11-languages.md), [0010](../roadmap/0010-protobuf-read-only.md) |
| D94 | protobuf's residue is stamped, never dropped: a field's wire number, a required label and a json_name, a message's reserved and extension ranges with their options, a file's imports with public and weak marked, its syntax or edition, its package, and the options every declaration level states, with edition features stamped apart from the other options. The well-known types project from a satellite table: wrappers as their scalar under the optional form, Empty, FieldMask, ListValue and Struct as the shapes they represent, and Any, Value and NullValue opaque. A group and an extend block are refused positioned | a schema language's meaning lives in what it states, and a generator reading a graph that lost a wire number or a presence feature generates something the schema did not say. Features are apart from the options because editions replaced the syntax keyword with them. The kernel registry blesses two identities on purpose, so the rest are a satellite's policy defaults | none | [11](11-languages.md), [0010](../roadmap/0010-protobuf-read-only.md) |
| D93 | Resolution is lexical and tiered: `ImportScope` gains `Owner`, the link phase walks a file's declarations under their own identities, and a language's `Resolve` returns `Candidates`, one tier per scope and innermost first. Link takes the first tier that names a declaration in the graph and reports an ambiguity only within that tier. A reference to a type parameter in scope targets the parameter, which the fold projects as opaque | a language whose scoping is lexical cannot resolve a nested name without the declaration a reference is written in, and one flat candidate list reports every legal shadowing as an ambiguity. A type parameter is a declaration in scope, and resolving its spelling against the package bound it to an unrelated type of the same name | none | [02](02-symbol-model.md), [11](11-languages.md) |
| D92 | The toolchain harness splits at one interface: the kernel owns the `Generated` fixture, the assertion set and its wording, and a satellite states only how to lay its language out, parse it, type-check it, test it and ask about satisfaction. A missing toolchain skips locally and fails in CI, decided by the `CI` variable through `Require`, which a satellite's own assertions call too. A fixture with no output and a test run with no case both fail | without a shared skeleton every language grows its own assertion vocabulary and they drift by hand. Parsing needs no toolchain, so grammar stays checkable everywhere, and the two absence refusals close the hole where a harness passes because it ran nothing | none | [13](13-testing-and-conformance.md), [0004](../roadmap/0004-go-loads-into-graph.md) |
| D91 | The scaffold's value expression is spelled by a shared walk over the tree with every syntax below it the target's own: a target states its literals, its conversion, its composite, its call and its address, and the walk states the recursion, the field forms and the refusals. A composite's three field forms, named, keyed and positional, cover a record, a map and a list, and the reference's form decides which syntax a target writes | four targets spelling one tree four times is the duplication the family helpers already removed once, and a target that reads the form off a type spelling rather than off the reference guesses. A language with no map literal or no address operator refuses through its own method instead of the walk inventing one | none | [RFC-0014](../rfc/0014-the-projection-vocabulary.md), [07](07-rendering.md) |
| D90 | A parameter and a return are dispatch subjects with identities under their callable's chain, named by position where unnamed; a type parameter takes an identity under its declaration's chain and stays no subject; a stamper writes to its subject and to the parameters, returns and type parameters the subject declares, through `StampOn`, and a rule gates on a kernel directive through `Gated` without carrying its schema | an authored sample on a parameter needs a subject to attach to and a rule to dispatch, a witness names a type parameter that has no carrier of its own, and the kernel registers its schemas once | none | [06b](06b-authoring.md), [RFC-0014](../rfc/0014-the-projection-vocabulary.md) |
| D89 | proto's residue is metadata and refusal: `protobuf.field` on a field, `oneof` as a Sum whose variants are the members, `protobuf.reserved` on a message, `protobuf.options` on a file, and `extend` refused with a positioned finding | each fact is a proto spelling with no neutral projection, and an extension changes a declaration the file does not own | none | [11](11-languages.md) |
