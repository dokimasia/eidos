# Decision log

*The index: every decision, its argument, and its revisit trigger.
Each context document holds the full reasoning.*

Every settled decision, its reasoning in one line (the full argument
lives in the linked context doc), and the recorded revisit trigger
where one exists. Each of these will be recorded as an ADR in
`docs/adr/`; this log is their source.

| # | Decision | Why (short) | Revisit trigger | Doc |
|---|---|---|---|---|
| D1 | One repository, one module per component: a kernel module + satellite modules, tagged independently; all implemented in Go | the module boundary follows change rate: models and contracts move slowly and their breaks are expensive; catalogs and language support churn on their own per-module cadence | — | [01](01-repos-and-kernel.md) |
| D2 | eidos is a library; no binary ever shipped | consumers' binaries are the products; command kernels give them a uniform surface for free | — | [14](14-distribution-and-cli.md) |
| D3 | Public-framework posture: stability promised, docs a product, conformance the instrument | external adoption is a goal, and it changes engineering, not just marketing: every schema and flag becomes API someone scripts against, so stability must be checked machinery rather than intention | — | [15](15-compatibility.md) |
| D4 | Two concrete models (node/emit), generated from one symbol schema | a merged model loses compile-time phase discipline and multi-plan safety; embedding dies on covariant recursion; generation deletes the mirror drift | — | [02](02-symbol-model.md) |
| D5 | Projection tiers with the placement law | without law, vocabulary accretes wherever the first asker stood | — | [03](03-projection.md) |
| D6 | Vocabulary anticipates Rust, Python, Java, Kotlin, PHP, C#, Swift now | retrofitting variance, async, and error models later touches every detector and every lowering; PHP forces no new element — its unions, attributes, and enums are already in the vocabulary | — | [03](03-projection.md), [11](11-languages.md) |
| D7 | protobuf is read-only, permanently | a schema language is read, never written; the anatomy makes read-only a complete shape | — | [11](11-languages.md) |
| D8 | One workspace, N plans; one backend per plan; plans are values, not plugins | per-plan invariants (imports, formatting, sweep) keep one answer; plans own what must be enforced between plugins | — | [08](08-workspace-and-plans.md) |
| D9 | No daemon, no client-server protocol; batch + `--watch`; mmap/lazy sealed-graph format | the envelope is met by format design; a protocol is public API plus state-drift tax, with no client that needs residency | an IDE surface actually being built | [09](09-incrementality.md) |
| D10 | Incrementality: red-green + early cutoff at (symbol, key) grain | fat bags × many plans make per-symbol invalidation wake unrelated plans; bookkeeping is O(reads) | — | [09](09-incrementality.md) |
| D11 | Cross-language is hub-and-spoke through canonical shapes; policy as data; no bridge modules | pairwise walkers are O(pairs) and drift; a hub makes each language map once | — | [10](10-cross-language.md) |
| D12 | No CUE anywhere; text/template + lint rung + builder dual; YAML + published JSON Schema | CUE does not render prose; config gets typing via schema without a new language for consumers | lowering-policy tables growing real constraint logic | [07](07-rendering.md) |
| D13 | No rebuild tool | its audience already has the Go toolchain; non-Go users are served by consumer binaries + the declarative host | demand from consumers who cannot ship binaries | [14](14-distribution-and-cli.md) |
| D14 | One directive grammar; per-language carriers; native sugar lowers to canonical | N dialects fracture plugins, docs, and migration; carriers keep idiom | — | [05](05-directives.md) |
| D15 | Shape catalog: spec-first, generated registries, projection-only detectors | under-determined directives and hollow laws become review-time checkboxes; 90 + N, not 90 × N | — | [12](12-shape-catalog.md) |
| D16 | tree-sitter is the frontend platform — one grammar model, one binding layer, well maintained, uniform across satellites; parsers are pinned libraries, never machine-supplied toolchains; two exceptions where a proven first-class pure-Go parser exists: Go stdlib `go/parser`, protobuf protocompile | the requirement is hermeticity and cross-machine determinism, with uniformity where it is free — a version-pinned parser is hermetic whatever its origin, the kit keeps the two exceptions invisible downstream, and the native TS compiler is written in Go but ships internal packages only, so there is nothing importable to pin | a native compiler port publishing a public importable API for a heavy source language | [11](11-languages.md) |
| D17 | Monorepo-grade envelope: 10k+ packages, sub-second warm, memory-resident + signature-only deps | the envelope is chosen, not derived: the largest consumers are monorepos, and a tool that degrades there gets replaced by five single-purpose generators; stating it as requirement is what licenses every engine trade-off downstream | — | [09](09-incrementality.md) |
| D18 | Workspace = config boundary spanning toolchain workspaces; multi-workspace explicit-only; nothing crosses | implicit nesting is the orphan shape: two manifest owners that don't know about each other | — | [08](08-workspace-and-plans.md) |
| D19 | Plan exports with declared dependencies; raw sibling-emit reads forbidden | binding generators need generated names without drift; undeclared edges kill scheduling and parallelism | — | [08](08-workspace-and-plans.md) |
| D20 | Per-role priorities | one shared number cannot place a dual-role plugin's halves independently | — | [06](06-plugins.md) |
| D21 | Interfaces, never implementations | the boundary that keeps conversion from becoming a compiler | — | [10](10-cross-language.md) |
| D22 | Stable diagnostic codes as API | consumers assert on codes, not prose; a changed meaning is a new code | — | [15](15-compatibility.md) |
| D23 | C/C++ out of scope | no declaration model worth projecting without a real compiler frontend; refused, not half-done | a funded compiler-frontend effort | [11](11-languages.md) |
| D24 | The foundation set, five decisions that stand together: compiled-in plugins; metadata as the only channel; slots; per-backend template ownership; funcmap registered once per language | each guards the others — typed keys and a shared graph exist only in-process (so compiled-in), facts need authority and provenance (so one channel), shared output needs accumulation not override (so slots), languages differ by structure (so backends own templates), and helpers need one documented name (so funcmap-once). Removing any one reopens the hole the next one covers | — | [06](06-plugins.md), [07](07-rendering.md) |
| D25 | Three severities; any Error fails the run; suppression is per-code-per-position with an audit trail; kernel Errors are unsuppressible | silent or global suppression hides real findings; codes make targeting precise; dead suppressions are surfaced by `doctor` | — | [16](16-diagnostics.md) |
| D26 | Sink contract: write-if-changed, atomic per file, staged per plan, root-jailed; no clocks or environment in output; UTF-8/LF everywhere | mtime churn poisons consumers' build caches; partial writes corrupt trees; timestamps break byte-determinism | — | [17](17-output-and-determinism.md) |
| D27 | Routing: two layouts + tagged outputs with per-plugin/per-tag refinement and kernel-owned per-declaration overrides; unmanifested files are never overwritten | source-level intent outranks config, consistent with the authority model; proving ownership before deletion or overwrite is what makes sweeping safe | — | [18](18-routing-and-layout.md) |
| D28 | Benchmark gating splits by machine-independence: scaling-law and allocation gates everywhere, absolute-time gates on dedicated runners with significance testing; corpora are seeded synthetic | timing on shared runners flakes and teaches people to ignore the gate; ratios and allocation counts are noise-free; real repos can't be parameterized | — | [19](19-benchmarking.md) |
| D29 | Frontends never execute project code or build tools; module identity that is only computable by execution is supplied in config | hermeticity and supply-chain safety; a generator that runs Gradle is a build step, not a reader | a sandboxed evaluator design | [11](11-languages.md) |
| D30 | Drift detection: a manifested file whose on-disk hash differs from the manifest is refused, not overwritten; prune never sweeps it | the manifest already holds content hashes, so hand edits are detectable — silent overwrite is data loss wearing regeneration | — | [17](17-output-and-determinism.md) |
| D31 | Signature-only dependency loading reads binary declaration formats where ecosystems distribute no source (JVM class files, .NET assembly metadata) — parsed, never executed | JVM dependencies are JARs; a source-only reader would parse the workspace and resolve nothing | — | [11](11-languages.md) |
| D32 | A per-release schema index with mechanical evolution classification; breaking schema changes block the tag outside a major; `doctor` runs the consumer-side diff | annotations in consumer source are the least absorbent surface; additive-only must be checked on schemas exactly as it is on APIs | — | [15](15-compatibility.md) |
| D33 | Per-plan model transforms refused; whole-file ownership only — eidos never merges into files it does not wholly own | transforms fork the one graph and break explain/invalidation/checks; partial file ownership has no determinism, sweep, or drift story | — | [08](08-workspace-and-plans.md), [17](17-output-and-determinism.md) |
| D34 | Plan exports are a versioned kernel schema recording kind, canonical signature, and the lowering's chosen spelling per symbol | the producing plan's lowering is the only authority on what it named; dependents read spellings, never recompute conventions | — | [08](08-workspace-and-plans.md) |
| D35 | Values projection in Tier 1: two distinct samples, zero literal, literal-for-text — all refusal-first | check generators live on distinguishable values: a check against one value passes whenever the subject already held it; a guessed literal is worse than a refusal | — | [03](03-projection.md) |
| D36 | The scaffolding body vocabulary is deliberately minimal: delegate call, return, assignment, guard — and grows only by kernel decision | it keeps "interfaces, never implementations" a mechanical boundary instead of a judgment call; logic belongs in hand-written files the generated one calls | — | [07](07-rendering.md) |
| D37 | Render dispatch: backend kind templates render everything by default; a plugin claims its own files per declared output; slot contents always render through kind machinery | assembly-only plugins ship no templates and still render idiomatically; no plugin's template ever renders another plugin's values | — | [07](07-rendering.md) |
| D38 | Mixing granularity: statements → body (`TemplateRef`, resolved in the emitter's tree at render time) → file — and nothing between declarations; `Verbatim` is body-only | generators name templates and never execute them, staying language-blind; declarations stay structured for slots, merging, imports, and lint; free-floating text among typed declarations defeats all four | — | [07](07-rendering.md) |
| D39 | Bodies are slot sequences: standard `prologue`/`epilogue` on every body, owner-declared slots between; body-claiming templates must place the marker (lint-checked); composition is three verbs — append (slots), replace (template overrides), wrap refused at render level and done at model level; slot declarations generate from the schema | cross-plugin body composition without touching the other plugin's template; standard slots remove the owner-must-anticipate weakness; render-level wrapping is order-sensitive semantics nothing can defend | — | [07](07-rendering.md), [02](02-symbol-model.md) |
| D40 | Canonical symbol identity: package path + kind + name + signature discriminator, stable across reparses | read-sets, invalidation, exports, provenance, drift, and explain all reference symbols across runs; without a stable identity the engine is unimplementable and overloads make names insufficient | — | [02](02-symbol-model.md) |
| D41 | Naming and lowering facts stamp during Annotate via auto-registered target-language annotators; plans never write the shared graph | plans run post-freeze, in parallel, read-isolated — a plan writing shared bags breaks all three; a name is a fact about (symbol, target language), not about a plan | — | [10](10-cross-language.md) |
| D42 | Adoption by byte-equality on unmanifested paths (`--adopt` for the rest); one exclusive writer per workspace, concurrent runs fail fast | adopting the tool must not fail on file one; two runs racing one manifest is the orphan shape with extra steps | — | [17](17-output-and-determinism.md) |
| D43 | The directive grammar is pinned in the spec: EBNF, one quoting form (double quotes, four escapes), trailing-`\` continuation, flat keys | the surface least able to absorb change gets the most exact specification; a deferred grammar is an invitation for carriers to diverge | — | [05](05-directives.md) |
| D44 | Boundary strings, interior constants: strings exist only where humans type; every registered name is a typed value or generated constant inside the type system; every boundary string resolves against its registry at workspace Build | it converts a whole typo class into compile errors where code is involved and into named Build errors at the boundary; generated constants mean nothing hand-written drifts from a registry | — | [README](README.md) |
| D45 | Two-layer plugin surface: the SPI roles stay the infrastructure contract; authors write the root-package facade — declarations as a value, triggers indexed by the symbol model's subject kinds (generated with the schema) plus `OnEmit`/`OnGraph`, `Directive`/`Where` scoping, effects chosen by handler signature, uniform `(match, effect) error` handlers, `skip` honored by dispatch; every rule set lowers to the SPI | the walk-filter-bookkeep ritual and its correctness lessons (receiver collision, import inference) move into the framework once instead of every plugin; kinds are structure and facts are meaning, so no semantic trigger names; the lowering is conformance-tested to byte-equal output and the anti-sprawl rule guards mechanisms, not kind coverage | a plugin family the kind-indexed triggers plus wrappers cannot express | [06b](06b-authoring.md) |
| D46 | Output cardinality is declared per family (`PerSource`/`PerPackage`/`PerPlan`); every emitter target is an accumulator keyed by (cardinality key, family), ordered by canonical subject identity; multiple files per plugin are families × keys; provenance is plural and independent of filename derivation | one-source-one-output was the demo's shape, not the domain's; even `PerSource` accumulates (two interfaces in one source file share one output); match-order-dependent aggregates would break warm≡cold under parallel dispatch | — | [18](18-routing-and-layout.md), [06b](06b-authoring.md) |
| D47 | Resolution is Link then Lookup: a kernel Link step after Load resolves type spellings to canonical identities (language-assisted, neutrally recorded, degrading to spelling-only for builtins and externals); `Reader.Lookup(Identity)` is the cross-file/cross-package join thereafter, flowing through tracked reads; textual names resolve via Tier-1 `Resolve` at validation; cross-plan resolution does not exist | cross-package resolution needs the whole graph, so it is a phase, not an on-demand walk; identity as the join makes dependency-package lookups identical to local ones and turns every cross-package read into an incrementality edge for free | — | [02](02-symbol-model.md) |
| D48 | No Transformer role: source rewriting, graph transformation, and emit rewriting are all refused; each use case maps to a composing mechanism (replace, model-level wrap, projections/facts, frontends, `manual`-authority tooling) | the graph is input truth and emitted values have one owner; every transform variant destroys determinism, provenance, or output stability under plugin addition — the mapping table gives askers their answer without reopening the argument | — | [06](06-plugins.md) |
| D49 | Plugin target-universality: neutral-lane plugins (builder + scaffolding + kind templates) serve every target; presentation layers as options — plugin-level `Templates`/`Funcs` defaults, `For(target, …)` specializations, `Overrides` as the replace verb; no per-target word map (filenames take the family word with target-spelled join/ext; identifiers go through the Emitter's `JoinName`); undeclared targets fail at Build; one directive gates many rules and registers once, conflicting redeclarations refused | the Source/Target type split applied to the authoring API itself — read-side on the Match, write-side on the Emitter; a target fact in a neutral declaration is a leak Go→Go development hides and cross-language plans expose; a word map is presentation smuggled into a side channel when templates and helpers are the presentation surface | — | [06b](06b-authoring.md) |
| D50 | Routing is addressed, never inferred: families are independent accumulators and the handler names the family per emit site; tags carry meaning and the target spells the (word, tag) join; `TagTest` is a kernel well-known tag targets may give ecosystem treatment (`suite_test.go` vs `suite.test.ts`), unknown tags get the deterministic default join | content-based routing would be guessing; `_test` in a declared word is the extension leak one level up — test-ness is semantics the plugin declares and the ecosystem spells | — | [18](18-routing-and-layout.md), [06b](06b-authoring.md) |
| D51 | Annotators are the same rule grammar with the Stamper effect: directive-drivable, fact-gatable, or bare; `Stamp` pre-binds authority and origin; the subject-only law (a Stamper writes only its subject's bag); tracked `Fact` reads; `Provides`/`Requires` surfaced on the builder | one grammar to learn, not two; subject-only writes make every write target statically known from the trigger, keeping invalidation and audit analyzable; a fact "about" a sibling is a fact on the host naming the sibling | — | [06b](06b-authoring.md) |
| D52 | Schemas declare repeatability: single-instance by default (duplicates are validation Errors naming both positions); `Repeatable` fires the handler per instance in source order; `m.Directive()` is always the gating instance (nil for bare/fact); other plugins' directives are invisible to a handler; ordering law extends to subject → instance order → insertion | contradiction vs repeatable declaration is per-directive semantics only the schema can settle; visibility keeps directive params from becoming de-facto public API for strangers; instance-order determinism survives parallel dispatch | — | [05](05-directives.md), [06b](06b-authoring.md) |
| D53 | The frontend kit: per-language `CommentSyntax` shared with the backend kit; include-shaped selection with classification over exclusion (test files parsed and stamped, never dropped); the language-defined Unit with `u.Read` as the only filesystem door — jailed, fingerprint-tracked; same `Parse` at `Depth Signatures` | comment extraction must survive doc-block gutters and docstrings across the landscape; exclusions hardcode consumer decisions; the VFS door turns D29 hermeticity and cache completeness from laws into mechanisms; the one kernel-owned exclusion is the workspace's own outputs (D62) | — | [11](11-languages.md), [09](09-incrementality.md) |
| D54 | The backend kit: `FileTemplate` skeleton (kit default), `KindTemplates`, `Funcs`, `Imports` renderer, `Finalise`, shared syntax for the 17-header; merging, splicing, `TemplateRef` resolution, and the `CodeRender` flow are kit-owned | import grouping and file skeletons are language facts, but the header law, format-error law, and merge order must be kernel behavior tested once — not divergently reimplemented per satellite | — | [11](11-languages.md) |
| D55 | The gate-free handler law: gates are declarative (`Directive`/`Where` compose over every trigger; `Where` over `OnEmit` evaluates the origin); dispatch is an indexed one-pass engine (by kind, by directive name at Freeze, by fact key at stamp); gate tuples double as the incrementality engine's subscription records | a gate inside a handler is scan-and-filter wearing a subscription costume: selectivity the engine cannot see degrades dispatch to O(graph) walks and invalidation to over-waking; a rule must cost O(its matches) | — | [06b](06b-authoring.md), [09](09-incrementality.md) |
| D56 | The performance commitments: stat-first fingerprinting (hash only on suspicion); the interning law (dense run-local IDs for identities, keys, and paths — strings boundary-only; read-sets as packed ID pairs, deduped per artifact); small-slice bags over interned key IDs; per-file render parallelism kit-owned; Link appears in per-phase bench attribution | the asymptotics are sound by construction — the residual risk is constants and memory, and each commitment kills the largest known constant in its layer before the RSS and time gates find it the hard way | — | [09](09-incrementality.md), [11](11-languages.md), [19](19-benchmarking.md) |
| D57 | Fact deletion is a kernel `meta` form: `meta drop=<key>` removes a fact at `directive` authority (outranks `plugin` stamps, loses to `manual`), visible to `explain` as an authored deletion | booleans stamp only when true and absence is the negative, so negating a stamped fact has no set-a-value spelling; without drop, the only recourse is `skip`, which withdraws the whole plugin instead of one fact | — | [04](04-metadata.md), [05](05-directives.md) |
| D58 | Tier-2 `EqualityRules`: `Comparable(ref, resolve) (ok, problems)` — is a type usable where the language demands equality, and which member refs poison it | generators that build map keys, switch arms, or dedup logic otherwise re-derive comparability per plugin and get the recursive cases (struct containing slice, two levels down) wrong; the language owns the rule and names the offending members for the diagnostic | — | [03](03-projection.md) |
| D59 | Two standing refusals: the ecosystem directive prefix is permanent (one prefix, never per-plugin or per-workspace configurable), and there is no symbol-level run narrowing (no "regenerate just this declaration" flag) | a configurable prefix forks the carrier grammar per workspace and breaks cross-repo tooling and grep-ability for zero expressive gain; symbol-level narrowing duplicates what dirty-driven regeneration already does — revisit only on a profiled workflow where dirty-driven regeneration is demonstrably not enough; the per-package `go generate` workflow is served by pattern narrowing (`run .`, D71), never by symbols | — | [05](05-directives.md), [09](09-incrementality.md) |
| D60 | The runtime architecture: immutable `Workspace` (composition, registries, compiled dispatch plan) vs per-invocation `Run` (ledger, graph, facts, dispatcher, plan executions, diag); seams are Go contracts (`Ledger`, `Graph`, `Facts`, `Dispatcher.MatchesOver`, `PlanExec`); two-phase commit (stage everything, then plan commits, ledger's `CommitRun` strictly last); sealed state is generational and includes the fact store; dry-run is a Run whose staging is discarded | an org-chart of components is not an architecture — the seams, the granule, and the commit protocol are; `CommitRun`-last makes every crash window conservative (worst case a cold-ish rerun, never a stale-marked-clean lie); persisting facts is what makes warm annotation O(dirty) instead of O(subjects) | — | [08](08-workspace-and-plans.md), [09](09-incrementality.md) |
| D61 | The artifact is the re-execution granule and checks read records: the ledger's artifact table (inputs hash over contributor tuples + read-set closures + versions, contributor set, output hash) decides dirtiness by lookup; dirty artifacts re-fire their whole contributor set, clean artifacts carry manifest rows and execute nothing; `WorkspaceCheck` reads manifests, exports, graph and facts — never raw emit | emit is never persisted, so any warm path that needs sibling emit is a design error — the artifact table answers "whose inputs moved" without rerunning anything, and record-based checks behave identically cold and warm, carried plans included | — | [09](09-incrementality.md), [08](08-workspace-and-plans.md), [06](06-plugins.md) |
| D62 | Outputs are never inputs: Load excludes workspace-owned paths (manifest entry or provenance trailer) at the fingerprint gate; foreign generated files are classified, never excluded | self-ingestion feeds generators their own output and breaks warm≡cold; the scope vocabulary has no negation, so no consumer could spell the exclusion; ownership proof is decidable per file, declared output globs are only a pre-filter (`out=` redirects and orphans of removed plugins match no declaration) | — | [08](08-workspace-and-plans.md), [09](09-incrementality.md), [11](11-languages.md) |
| D63 | The provenance trailer: every file ends with `<brand>:provenance sha256:<body-hash>`; overwrite and delete require ownership proof (trailer or manifest entry); the manifest lives uncommitted in the brand state dir; headers carry brand + source, never the command line | ownership in the bytes survives clones where state does not — first-run exclusion, adoption, drift, and CI gates all work manifest-less; a committed manifest is a merge-conflict generator; a command-stamped header varies with phrasing and defeats write-if-changed | — | [17](17-output-and-determinism.md), [20](20-cli.md) |
| D64 | Authored values: kernel-owned `sample` and `witness` directives stamp kernel-owned `gen.*` keys; Values and Witnesses answers consult the stamps before deriving; handlers ship as default annotators | witnesses derive only from trivially-closed constraints (`any`, `comparable`) — everything else is authored or every check over the declaration is withheld; the preference seam must be contract or satellites fork on it | — | [03](03-projection.md), [05](05-directives.md) |
| D65 | Tier-1 `Members`: the effective member set across Embeds/Extends/Implements, with per-member contributor provenance and per-contributor failure reasons | the first question every doubling generator asks; promotion, override, merge, and MRO are language rules, so anywhere but the language contract re-derives them wrong; the failure reasons let a generator refuse an incomplete double instead of shipping it silently | — | [03](03-projection.md) |
| D66 | Fact groups: key registration may declare a bundle name; `meta drop` accepts a key or a group and tombstones every member, later stamps included | facts ship in families; enumerating a writer's keys couples authors to internals the visibility law hides; a declared group is one public name, registry-resolved, typo-checked at Build | — | [04](04-metadata.md), [05](05-directives.md) |
| D67 | Schemas declare cross-directive `Requires` / `ConflictsWith`, kernel-validated at Freeze over each subject's full directive list, both positions named | the visibility law leaves no plugin able to check a stranger's annotation, so contradictions between annotations are caught by the schema layer or not at all | — | [05](05-directives.md) |
| D68 | No panic recovery: a plugin panic crashes the run with the plain stack | a panic is a defect, not a diagnostic; staging makes the crash window conservative (nothing partial lands) and plugintest is where panics are caught — recovery converts defects into ignorable warnings on a run whose remaining output lost its witness | — | [16](16-diagnostics.md) |
| D69 | Centralised package identity derives from frontend-stamped module facts, with `importBase` config where no module contains the outdir; refusal lands at the referencing declaration, only when qualification is actually needed | an import path is not derivable from a directory alone; a guessed path binds bare names silently — the one failure a compiler will not catch; refusal at the point of need costs reference-free outputs nothing | — | [18](18-routing-and-layout.md) |
| D70 | Brand-scoped discovery and state: `.<brand>.yaml` config, `.<brand>/` state dir beside it, lock per state dir | no eidos binary exists to claim `eidos.yaml` (D2); two consumer binaries in one repo must not read each other's config or race each other's manifest — cross-brand file safety is the ownership law (D63), not the lock | — | [20](20-cli.md), [17](17-output-and-determinism.md) |
| D71 | Narrowed-run sweep: a scoped run sweeps only inside its narrowed source scope (or on producer removal); out-of-scope manifest entries carry forward; `prune` reconciles the whole workspace; `run .` under `go generate` is the supported per-package workflow | a partial run must not eat outputs it merely didn't look at; `go generate` executes in the package directory, so pattern narrowing serves the workflow without reopening D59 | — | [08](08-workspace-and-plans.md), [20](20-cli.md) |
| D72 | Sealed-state design: one generation directory (graph = intern table + per-unit region index + regions; fact store; artifact table; gating header), `CURRENT` pointer swap; region = the frontend unit; lazy regions are the contract, mmap only a permitted mechanism; stdlib-only encoding; mismatch or corruption falls back to cold with one Info | the parse, lock, and invalidation granules collapse onto one grain; state is an accelerator, and an accelerator with veto power is a dependency — damaged state must never fail a run source alone could complete | — | [09](09-incrementality.md) |
| D73 | Module identity is a kernel-neutral fact: every frontend stamps `gen.module` / `gen.moduleRoot`; raw toolchain spellings stay in the frontend's own namespace; Sources predicates, layout, and lowerings consume only the neutral keys | kernel machinery (scope matching, centralised layout) must read module identity, and the kernel knows no language — per-language keys would put a language table inside it; normalize-plus-raw is the same move as Visibility | — | [08](08-workspace-and-plans.md), [18](18-routing-and-layout.md) |
| D74 | Facts live on the source graph only: `OnEmit` takes the Emitter effect alone, and Build rejects an `OnEmit` rule with a Stamper handler | emit is per-plan and plans run in parallel — emit-side facts would be a second universe invisible to Close's records, the sealed state, and sibling plans; a fact about generated output is a fact on its origin, stamped during Annotate | — | [06b](06b-authoring.md) |
| D75 | Exit codes: 0 success, 1 diagnostics failed, 64 usage/config (`EX_USAGE`); 2 is deliberately unclaimed — a Go panic exits 2, so a crash is distinguishable from a typo without parsing stderr | with D68 (no panic recovery) a panic's exit collides with any meaning assigned to 2; leaving 2 to the runtime makes every code mean exactly one thing | — | [16](16-diagnostics.md), [20](20-cli.md) |
| D76 | Build is a specified phase: a seven-step validation ladder (registries → lowering → options → plans → policies → dispatch compilation → contract handshake) that collects every fault and reports all at once; the workspace builder, `Plan`, and `ExportDoc` are pinned contracts | the most-referenced phase in the spec was the only unspecified one; ordering matters because later steps assume resolved names, and collect-all is what lets a consumer fix a composition in one pass | — | [08](08-workspace-and-plans.md) |
| D77 | The parse memo: a second, optional persistence layer — content-addressed regions keyed by (unit fingerprint, frontend version), consulted by Load between sealed region and reparse; size-capped LRU enforced at CommitRun; `run --cold` bypasses both layers; CI restores are safe by design; no remote or shared cache protocol | the sealed state answers the edit loop, the memo answers branch-hopping and restored CI caches; hit-equals-fresh-parse is a tautology of the key; an uncapped cache is a disk leak, and a cache protocol is the daemon argument wearing a network | an organisation demonstrably saturating CI-restore bandwidth | [09](09-incrementality.md), [08](08-workspace-and-plans.md), [20](20-cli.md) |
| D78 | The Reader's grain rule: targeted reads (Lookup, member walks, facts) record per-identity edges; enumerations (ByKind, package lists) record set-membership edges — added/removed wakes enumerators, mutation wakes only targeted readers; edges are scope-filtered | the gate-free law's economics extended to reads — no read costs more than it observed; without the rule, either every enumeration wakes on any mutation (over-waking at L scale) or invalidation silently misses additions | — | [02](02-symbol-model.md), [09](09-incrementality.md) |
| D79 | `run --check` is the CI staleness gate: dry-run semantics with a non-empty diff promoted to an Error diagnostic, exiting 1 through the ordinary contract | dry-run exits 0 on a pending diff by design (a diff is not an error at review time), so CI would otherwise parse JSON to enforce currency; promoting to a diagnostic reuses the exit contract instead of minting a code | — | [20](20-cli.md) |
| D80 | The catalog is one classification mechanism with three declared forms, pinned by a `form` field in every spec: shape (single-valued, inferable, precedence), mixin (declared, accumulating), contract (declared, grouped roles with arity); name constants are typed per form | the forms are the occupied cells of who-asserts × how-many-stick, and the form decides what nothing downstream can re-derive — arbitration (first-wins vs accumulate), directive shape, and what a consumer generates; collapsed to one flat namespace, every consumer carries a which-kind lookup and first-claim-wins breaks for mixins | — | [12](12-shape-catalog.md) |
